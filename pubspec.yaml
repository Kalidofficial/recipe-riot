name: project
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.6
  firebase_core: 3.3.0
  firebase_auth: ^5.1.3
  cloud_firestore: ^5.0.0  # Add this line for Firestore support
  url_launcher: ^6.1.6
  provider: ^6.0.0
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.10.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/recipelogo.png
    - lib/assets/images/background.jpg
    - lib/assets/images/bg.jpg
    - lib/assets/images/hbg.jpg
    - lib/assets/images/cbg.jpg

flutter_icons:
  android: true
  ios: true
  image_path: "lib/assets/images/appicon.jpg"

